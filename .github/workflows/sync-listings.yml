name: Sync apartment listings

# Runs every hour: adds new listings (Craigslist, Redfin, ...) matching
# data/search-criteria.json and marks any listings that have been taken down as
# off-market. Commits the updated data/apartments.json back to the repo so the
# site picks it up.

on:
  schedule:
    - cron: '0 * * * *' # top of every hour (UTC)
  workflow_dispatch: {} # allow manual runs from the Actions tab

permissions:
  contents: write

concurrency:
  group: sync-listings
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Sync listings (add new + off-market sweep)
        run: node scripts/sync-listings.mjs

      - name: Commit & push changes
        run: |
          if [[ -n "$(git status --porcelain data/apartments.json)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/apartments.json
            git commit -m "Auto-sync listings (add new, mark off-market)"
            git push
          else
            echo "No changes to commit."
          fi
